# Java-Calculator
Simple GUI calculator compatible with the four basic operations (+, -, *, /).

Functioning calculator using a simple GUI interface. Supports addition, subtraction, division, and multiplication. Also supports the use of parentheses.
Also supports negative numbers, and displays the result of the previous computation, so the result can be used for further operations.
